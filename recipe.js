(()=>{const all=window.SHAKTI_RECIPES||[],grid=document.getElementById('recipeGrid'),search=document.getElementById('recipeSearch'),filter=document.getElementById('recipeFilter'),count=document.getElementById('recipeCount'),dialog=document.getElementById('recipeDialog'),detail=document.getElementById('recipeDetail');const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const url=r=>new URL('recipes.html?recipe='+encodeURIComponent(r.slug),location.href).href;const qr=r=>'https://api.qrserver.com/v1/create-qr-code/?size=220x220&margin=8&data='+encodeURIComponent(url(r));function openRecipe(r,push=true){detail.innerHTML='<article class="detail"><div class="detail-media"><img src="'+esc(r.image)+'" alt="'+esc(r.name)+' prepared dish"><div class="qr"><img src="'+qr(r)+'" alt="QR code for '+esc(r.name)+'"><small>Scan to open this recipe on mobile</small></div></div><div class="detail-copy"><span class="pill">'+esc(r.processing)+'</span><h1>'+esc(r.name)+'</h1><p class="product">'+esc(r.product)+'</p><div class="facts"><span><b>Water:</b> '+esc(r.water)+'</span><span><b>Soak:</b> '+esc(r.soak)+'</span><span><b>Cook:</b> '+esc(r.cook)+'</span><span><b>Serves:</b> '+esc(r.serves)+'</span></div><h3>Ingredients</h3><ul>'+r.ingredientsList.map(x=>'<li>'+esc(x)+'</li>').join('')+'</ul><h3>Method</h3><ol>'+r.stepsList.map(x=>'<li>'+esc(x)+'</li>').join('')+'</ol><p class="tip"><b>Tip:</b> '+esc(r.tip)+'</p><div class="share-row"><button id="copyRecipe">Copy recipe link</button><a target="_blank" rel="noopener" href="https://wa.me/?text='+encodeURIComponent(r.name+' — '+url(r))+'">Share on WhatsApp</a></div></div></article>';dialog.showModal();if(push)history.replaceState(null,'','?recipe='+encodeURIComponent(r.slug));document.getElementById('copyRecipe').onclick=()=>navigator.clipboard.writeText(url(r));}function render(){const q=search.value.toLowerCase().trim(),f=filter.value;const rows=all.filter(r=>(!f||r.processing===f)&&(!q||(r.name+' '+r.product+' '+r.processing).toLowerCase().includes(q)));count.textContent=rows.length+' recipes';grid.innerHTML=rows.map(r=>'<article class="recipe-card"><img loading="lazy" src="'+esc(r.image)+'" alt="'+esc(r.name)+' prepared dish"><div class="recipe-body"><span class="pill">'+esc(r.processing)+'</span><h2>'+esc(r.name)+'</h2><p class="product">'+esc(r.product)+'</p><p class="meta">'+esc(r.difficulty)+' · Serves '+esc(r.serves)+'</p><button data-slug="'+esc(r.slug)+'">View recipe & QR</button></div></article>').join('');grid.querySelectorAll('button').forEach(b=>b.onclick=()=>openRecipe(all.find(r=>r.slug===b.dataset.slug)));}search.oninput=render;filter.onchange=render;dialog.querySelector('.close').onclick=()=>{dialog.close();history.replaceState(null,'','recipes.html')};dialog.onclick=e=>{if(e.target===dialog){dialog.close();history.replaceState(null,'','recipes.html')}};render();const slug=new URLSearchParams(location.search).get('recipe');const initial=all.find(r=>r.slug===slug);if(initial)openRecipe(initial,false);})();

